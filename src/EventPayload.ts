export interface EventPayload {
    id                : string;
    type              : string;
    preferences       : string;   
    loadFactor        : string;
    items             : string;
    enable            : string
}