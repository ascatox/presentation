export interface EventPayload {
    id                : string;
    type              : string;
    preferences       : string;   
    loadFactor        : number;
    items             : string;
}